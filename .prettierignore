# 생성물은 포맷 대상이 아니다.
# pnpm-lock.yaml: lint-staged 의 *.{json,md,yml,yaml} 글롭에 걸려 커밋마다 prettier 가 재포맷하고
#   pnpm install 이 다시 pnpm 원형으로 되돌리는 왕복 churn 이 생긴다(한 줄 변경이 수천 줄 diff 로 부풀어
#   리뷰와 머지를 방해한다). pnpm 이 유일한 소유자이므로 제외한다.
pnpm-lock.yaml

# 빌드 산출물: `pnpm format`(prettier --write .)이 dist 까지 훑는 것을 막는다.
dist

# Prisma 생성 클라이언트: 커밋/포맷 대상이 아니다 (`pnpm prisma generate`로 생성).
apps/api/src/generated

# Northflank 템플릿: GitOps 양방향 동기화 대상이라 Northflank 봇이 직접 커밋한다.
#   봇은 파일 끝 개행 없이 쓰는데 pre-commit 의 *.json prettier 가 다시 넣어 pnpm-lock.yaml 과
#   같은 왕복 churn 이 생긴다. 포맷 소유권을 Northflank 에 넘긴다.
apps/api/northflank.json

# TourAPI 지역코드 맵: 모바일의 generated JSON 에서 변환한 생성 파일이다.
#   스크립트가 만드는 형식이라 prettier 가 다시 손대면 재생성할 때마다 diff 가 생긴다.
apps/api/src/tourism/region-code-map.data.ts
