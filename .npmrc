# pnpm workspace 설정
# 빌드/린트/타입 툴체인은 루트 devDependencies로 모으고(hoist) 패키지에서 참조한다.
link-workspace-packages=true
save-workspace-protocol=rolling
strict-peer-dependencies=false

# pnpm deploy(modern) 활성화
inject-workspace-packages=true
