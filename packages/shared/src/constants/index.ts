/**
 * Tripic 공통 상수 (PRD 6.4 관광지 후보 조회 기준)
 */

/** 기본 검색 반경 (m) */
export const DEFAULT_RADIUS_M = 300;

/** 후보가 없을 때 확장 반경 (m) */
export const EXPANDED_RADIUS_M = 1000;

/** 후보 최대 노출 개수 */
export const MAX_CANDIDATES = 5;

/** 대한민국 시·도 총 개수 (지도 진행률 표시용, PRD 6.8) */
export const TOTAL_REGIONS = 17;
