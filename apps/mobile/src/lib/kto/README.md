# 한국관광공사 OpenAPI 경계

이 디렉터리의 클라이언트는 앱에서 한국관광공사 OpenAPI를 직접 호출한다.

- 좌표는 후보 조회 요청을 위해서만 메모리에서 사용한다.
- OpenAPI 응답과 관광지 목록은 SQLite에 저장하지 않는다.
- API 키 주입 방식은 배포 환경을 확정한 뒤 별도 검토한다. 키를 앱 소스나 `.env`에 커밋하지 않는다.
